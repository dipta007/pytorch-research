# https://docs.wandb.ai/guides/sweeps/configuration
entity: dipta007
project: pytorch-research
program: train.py
method: bayes
metric:
  goal: minimize
  name: val/ppl
parameters:
  a:
    values: [1, 2, 3, 4]
early_terminate:
  type: hyperband
  s: 2
  eta: 3
  max_iter: 4
command:
  - ${env}
  - python
  - ${program}
  - ++debug=False
  - ${args_no_hyphens}
